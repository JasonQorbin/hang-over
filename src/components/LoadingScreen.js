/**
 * Simple functional component to display a loading screen while the application is loading up the dictionary
 */
function LoadingScreen() {
    return (
        <div>
            Loading...
        </div>
    )
}

export default LoadingScreen;